<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <%- include('../../partials/navbar', { user: user }) %>

    <h2><%= title %> (#<%= user.user_id %>)</h2>

    <% if(error) { %>
    <div style="color: red"><%= error %></div>
    <% } %>

    <form method="POST" action="/admin/users/<%= user.user_id %>/edit">
      <label
        >Username:
        <input
          type="text"
          name="username"
          value="<%= user.username %>"
        /> </label
      ><br />
      <label
        >Password (leave blank to remain unchanged):
        <input type="password" name="password" /> </label
      ><br />
      <label
        >Email:
        <input type="email" name="email" value="<%= user.email %>" /> </label
      ><br />
      <label
        >Role ID:
        <input
          type="number"
          name="role_id"
          value="<%= user.role_id %>"
        /> </label
      ><br />

      <button type="submit">Save</button>
    </form>
  </body>
</html>
