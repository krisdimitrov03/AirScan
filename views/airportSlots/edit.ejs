<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Edit Airport Slot</title>
  </head>
  <body>
    <h1>Edit Airport Slot</h1>

    <p>
      <a href="/airport-slots">Back to Airport Slots</a>
    </p>

    <form action="/airport-slots/<%= airportSlot.slot_id %>?_method=PUT" method="POST">
      <div>
        <label for="airport_code">Airport Code:</label>
        <input
          type="text"
          id="airport_code"
          name="airport_code"
          maxlength="10"
          value="<%= airportSlot.airport_code %>"
          required
        />
      </div>

      <div>
        <label for="date">Date (YYYY-MM-DD):</label>
        <input
          type="date"
          id="date"
          name="date"
          value="<%= airportSlot.date %>"
          required
        />
      </div>

      <div>
        <label for="time_slot_start">Start Time (HH:MM:SS):</label>
        <input
          type="time"
          id="time_slot_start"
          name="time_slot_start"
          value="<%= airportSlot.time_slot_start %>"
          required
        />
      </div>

      <div>
        <label for="time_slot_end">End Time (HH:MM:SS):</label>
        <input
          type="time"
          id="time_slot_end"
          name="time_slot_end"
          value="<%= airportSlot.time_slot_end %>"
          required
        />
      </div>

      <div>
        <label for="slot_capacity">Slot Capacity:</label>
        <input
          type="number"
          id="slot_capacity"
          name="slot_capacity"
          min="0"
          value="<%= airportSlot.slot_capacity %>"
        />
      </div>

      <div style="margin-top: 1em">
        <button type="submit">Update</button>
      </div>
    </form>
  </body>
</html>
